import {LearningGoalPipe} from './learning-goal.pipe';

describe('LearningGoalPipe', () => {
  it('create an instance', () => {
    const pipe = new LearningGoalPipe();
    expect(pipe).toBeTruthy();
  });
});
